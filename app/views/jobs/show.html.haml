%h1= @job.title
%p= @job.description
%p= @job.company

%p= link_to "Home", jobs_path
%p= link_to "Edit", edit_job_path
%p= link_to "Delete", job_path, method: :delete, data: {confirm: "Are you sure?"}